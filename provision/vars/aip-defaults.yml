# CAIP version
cast_aip_version: 8.3.11.1940

# Release zip is stored in s3
# S3 bucket
cast_aip_s3_bucket: casthighlight-ci
# S3 objects keys (and local file names)
cast_aip_console_file: AIP-Console-1.8.1-SNAPSHOT-1229.jar
cast_aip_file: "CAST_AIP_{{cast_aip_version}}.zip" 

# Install base dir for cast prooducts on Windows
cast_opt: 'C:\\OPT'
cast_aip_install_dir: '{{cast_opt}}\\CAST_AIP'
